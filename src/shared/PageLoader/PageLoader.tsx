import { AvatarIcon } from "../../icons/svg.custom";
import "./PageLoader.scss";

export const PageLoader = (props: any) => {
  return (
    <div className="page-background primary-background">
      <AvatarIcon height={100} />
    </div>
  );
};
